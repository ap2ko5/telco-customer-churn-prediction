"""
src/__init__.py
===============
Makes `src/` a proper Python package so it can be installed with pip install -e .
and imported cleanly in tests and scripts as:

    from src.config import MODELS_DIR
"""
